apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`argocd-oci.naser.cloud`)
      priority: 10
      services:
        - name: argocd-server
          port: https
    - kind: Rule
      match: Host(`argocd-oci.naser.cloud`) && Header(`Content-Type`, `application/grpc`)
      priority: 11
      services:
        - name: argocd-server
          port: https
          scheme: h2c
  tls:
    secretName: letsencrypt-wildcard-cert-naser.cloud
