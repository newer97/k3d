apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: test-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: test-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: test-secret
