apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: onepassword
  namespace: external-secrets
spec:
  provider:
    onepasswordSDK:
      vault: k3s-oci
      auth:
        serviceAccountSecretRef:
          name: onepassword-connect-token-k3s-oci
          key: token
