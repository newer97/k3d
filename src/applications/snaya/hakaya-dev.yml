apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hakaya-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default
  source:
    repoURL: https://github.com/newer97/k3s-oci
    targetRevision: master
    path: src/apps/snaya/hakaya-dev/
  destination:
    server: https://kubernetes.default.svc
    namespace: hakaya-dev
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
